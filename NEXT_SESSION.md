# Tierly — pendientes próxima sesión

## Confirmado roto por el usuario (2026-08-26)
- ~~**Vista Perfil muy pobre**~~ **RESUELTO (2026-08-26)**: rediseñada siguiendo el mockup del usuario (`Tierly Gaming Profile Dashboard.png`) usando datos reales de Supabase. Layout: `.lb-profile-top-grid` (tarjeta de cuenta/`#lb-auth` + badge de rango + 7 stat tiles reales: posición, puntos, eventos jugados, premios, victorias, top 3, win rate) + `.lb-profile-grid` (historial reciente de brackets con puntos por partida + tarjeta de stats de Stellar Passport). Verificado en navegador con Playwright con sesión real logueada, sin errores de consola. **Deuda conocida**: no hay `player_id`/`discord_id` en las vistas públicas, así que el jugador actual se identifica matcheando `display_name` (trim+lowercase) contra el nombre de sesión de Discord — no es una key garantizada única si dos jugadores comparten el mismo nombre de Discord. **No implementable** (mockup lo pedía, sin dato real que lo respalde): racha/streak, logros/achievements, editar perfil, "miembro desde" — `event_bracket_public_view`/`leaderboard_public_view` no exponen esos campos. `pointsForPlacement()` en `app.js` duplica manualmente la fórmula de `points.mjs` porque `points.mjs` no se carga como script en `index.html` (mismo patrón ya usado para `GAMING_TIERS`/`ranks.mjs`).
- **Sistema de rangos por puntos con divisiones (2026-08-26)**: agregado en `tierly/ranks.mjs` (módulo puro, testeado en `tests/leaderboard-ranks.test.mjs`) + duplicado inline en `tierly/app.js` (mismo patrón que `calculatePoints`/`points.mjs`, ya que `app.js` no es un módulo ESM). 5 rangos sobre `total_points` (fórmula 10/6/3/1 por evento), cada uno con 3 divisiones (1/2/3) con su propio umbral — pedido explícito del usuario ("que este bronce 1, 2 y 3 y asi para cada rango, y que cada uno tenga como un puntaje"):
  - Bronce: 1→0, 2→170, 3→340
  - Plata: 1→500, 2→670, 3→840
  - Oro: 1→1000, 2→1230, 3→1470
  - Platino: 1→1700, 2→2100, 3→2500
  - Diamante: 1→3000, 2→3500, 3→4000 (techo — no hay rango después de Diamante 3)
  - **Modal "Ver rangos" (2026-08-26)**: botón `#lb-ranks-info-btn` junto al título del leaderboard (`Ver ranking` view) abre `<dialog id="lb-ranks-modal">` con explicación de cómo funcionan los rangos, la fórmula de puntos por evento, y los 5 rangos con sus 3 divisiones y umbrales (contenido generado en JS desde `GAMING_TIERS`, sin duplicar datos en el HTML). Cierra con el botón X o clickeando el backdrop. Verificado en navegador (abre centrado, cierra, sin errores de consola).
  Assets reales completos: `tierly/ranks/{bronze,silver,gold,platinum,diamond}.png` (copiados de `tierly/assets 38/Ranks/` — `platinium.png`→`platinum.png`, `diamante.png`→`diamond.png`). ~~FALTA assets de Platino y Diamante~~ **RESUELTO (2026-08-26)**: el usuario los pasó, ya wireados en `GAMING_TIERS` (`ranks.mjs` + copia en `app.js`), ya no queda ningún rango con ícono de fallback genérico.
- **Búsqueda de Passport builders no funciona**: nada de lo implementado en `passport-profile` (acción `builders`) devuelve resultados en `#lb-passport-results`. Revisar si el edge function actualizado llegó a deployarse (ver punto pendiente más abajo) y si la sesión/token se está mandando bien desde el frontend.
- **Link de Passport mal ubicado**: hoy el link para vincular Passport se ve en el leaderboard, pero debería estar en la vista Perfil (junto con el resto del bloque de auth/passport que se movió ahí).
- ~~**Stats de Passport no aparecen en Perfil**~~ **RESUELTO (2026-08-26)**: causa raíz — mismatch de campos entre la API pública de Passport (`avatar_url`, `bio`, `website_url`, `stats.totalCommits30d/activeDays30d`, `projects[]`) y lo que el frontend leía (`logo_url`, `description`, `website`, `b.stamp_count` inexistente). Fix: `normalizeBuilderResponse` ahora mapea `logo_url`/`description`/`website`, la acción `profile` devuelve `project_count`, y `renderPassportStats` renderiza chips reales (tier, proyectos, commits 30d, días activos) leyendo `data.stats` con fallbacks defensivos (`b.logo_url || b.avatar_url`, etc.) por si el edge deployado es viejo. **FALTA: deployear `passport-profile` (`supabase functions deploy passport-profile`) — sin eso solo se verán avatar/bio/website, no los chips de stats nuevos ni el avatar en las sugerencias.**

## Verificado en esta sesión
- Shape real de `https://demo.stellarpassport.xyz/api/builder/public/{username}`: `{builder: {github_username, avatar_url, display_name, bio, website_url, scf_tier, location, role_title, ...}, projects: [...], stats: {totalCommits30d, activeDays30d, lastActiveDate}, orgTiers: [...]}`. Tests de regresión en `tests/passport-profile.test.mjs`.
- `npm test`: 133/133 verde. Cache-bust de `tierly/app.js` bumpado a `?v=20260826-04`.
- Pendiente de deploy: `passport-profile` (normalización + role_title/socials/top_repos nuevos) y `discord-verify` (error 500 cuando falla la persistencia — cambio sin commitear todavía al igual que los demás).
- **Enriquecimiento de Passport en Perfil (2026-08-26)**: `passport-profile` acción `profile` ahora también devuelve `top_repos` (hasta 2, aplanados desde `projects[].repos[]`, shape `{full_name, html_url, stars, primary_language}`). `role_title`, `twitter_handle`, `telegram_handle`, `discord_handle`/`discord_username` y `visibility` ya venían en el builder normalizado (spread), así que `renderPassportStats` en `app.js` los lee directo — sin tocar `normalizeBuilderResponse`. Socials se ocultan si `visibility.<campo> === false` explícito (default visible si falta el flag, porque la API v1 autenticada no siempre trae `visibility`). Cache-bust `app.js?v=20260826-13`. Tests en `tests/passport-profile.test.mjs`. **FALTA deployear `passport-profile` y probar en navegador logueado.**

## Verificado en navegador (2026-08-26, sesión Discord real kl0ren)
- **Profile view** (`tierly/index.html` data-view="profile"): confirmado con Playwright, sesión ya logueada. Se ve el pill "Sesión iniciada como kl0ren", "✓ Verificado con Discord", link a Perfil de Stellar Passport, badge de rango (Bronce 1, 170 pts para Bronce 2), tarjeta de Passport con bio/nivel/proyectos/commits(30d)/días activos y socials (X, Telegram, Discord handle), 6 stat tiles reales (posición #1, puntos 0, eventos 0, premios 0, victorias 0, top3 0, win rate 0%), historial reciente vacío ("Todavía no jugaste ninguna partida"). Sin errores de consola.

## Sin verificar en navegador (no lo pude probar yo)
- **Búsqueda de Passport** (`supabase/functions/passport-profile` acción `builders`): pagina `/builders` (hasta 3 páginas de 100) filtrando por `name`/`username` porque la API no tiene búsqueda de texto. Requiere sesión activa (usa el token del usuario logueado). Falta: probar con un usuario logueado buscando un builder que NO esté en el leaderboard, y confirmar que aparece en `#lb-passport-results`.
- Falta desplegar el edge function `passport-profile` actualizado a Supabase (`supabase functions deploy passport-profile`) si no hay CI/CD automático.

## Deuda técnica / riesgos
- La búsqueda de Passport pagina hasta 300 builders por búsqueda (3 requests de 100). Si la base de Passport crece mucho, esto puede no encontrar coincidencias fuera de las primeras 300 o pegar contra el rate limit (100 req/min) si varios usuarios buscan a la vez. Si Passport agrega un parámetro `q`/`search` de verdad, cambiar `supabase/functions/passport-profile/index.ts` para usarlo en vez de paginar a mano.
- Sin caché de resultados de builders — cada tecleo (con debounce de 300ms) dispara requests nuevos. Si se nota lento o cerca del rate limit, considerar cachear la lista completa de builders en memoria del edge function con TTL corto.

## Discord bot (`discord-bot/`)
- Servicio standalone nuevo, separado del edge function `discord-verify`. Falta definir dónde se despliega (¿Railway, Fly, VPS?) y documentar variables de entorno requeridas más allá de `DISCORD_BOT_TOKEN`/`DISCORD_GUILD_ID` si las hay.

## Housekeeping
- `leaderboard/` y `ops/leaderboard/` quedaron renombrados a `tierly/` y `ops/tierly/` en este commit — revisar que no queden links rotos a `/leaderboard` en otras páginas del sitio (navegación, footer, etc.) ni en Vercel rewrites (`vercel.json`, `serve.json`).
